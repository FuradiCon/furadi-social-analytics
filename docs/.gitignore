# Screenshot tooling dependencies (restore with `npm install` in docs/)
node_modules/

# Screenshot output from the design/QA loop
temporary screenshots/

# Generated by the pipeline on every run — never committed.
# The site fetches this at runtime; it is published, not versioned.
data.json
