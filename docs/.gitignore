# Screenshot tooling dependencies (restore with `npm install` in docs/)
node_modules/

# Screenshot output from the design/QA loop
temporary screenshots/

# NOTE: data.json is deliberately NOT ignored. It is the published deliverable
# that GitHub Pages serves, and the hourly workflow commits updates to it.
